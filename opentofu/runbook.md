▶️ Commandes
bashtofu init       # Initialiser (une seule fois ou après ajout de provider)
tofu plan       # Prévisualiser ce qui va être créé / modifié / détruit
tofu apply      # Déployer
tofu destroy    # Tout détruire (attention en prod)